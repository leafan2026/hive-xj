{
  "name": "hive",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "preview": "node scripts/preview.mjs",
    "deploy": "wdl deploy . --ns lf",
    "dry-run": "wrangler deploy --dry-run --outdir=.deploy-dist"
  },
  "devDependencies": {
    "@wdl-dev/cli": "^1.6.1",
    "wrangler": "^4.102.0"
  }
}
