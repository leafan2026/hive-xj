# 复制成 .env 再填真值。`.env` 已在 .gitignore 里，`.env.example` 才是提交的那份。
#
# 这里放的是**部署凭证**（ADMIN_TOKEN，用来把代码推到 WDL 控制面）。
# 运行时密钥（金数据 API key 之类）一律走 `wdl secret put`，绝不进这个文件。
#
# 为什么需要它：wdl 默认把 token 存在**运行它那台机器的 home**（`wdl token set`，
# 落在 ~/.config/wdl/credentials）。换机器、重装、或在会话沙箱里跑时那份就没了。
# 放一份在项目根目录的 .env 里，跟着仓库目录走，不用每次重新给 token。
#
# 注意 CLI **只读运行 wdl 时所在目录下的 ./.env**，不会往上层目录找 ——
# 所以要在 workers/hive/ 里执行 wdl，不是在仓库根目录。
# 优先级：CLI 标志 > shell 环境变量 > .env 的 [<ns>] 段 > .env 基础段 > wdl token store。
# 拿不准最终取了哪个值就跑 `npx wdl config explain`。
#
# 跟 zlb（qa-tickets）是同一个 namespace、同一个控制面、同一个 token。

WDL_NS=lf
CONTROL_URL=https://admin-run.jinapp.net

# token 按命名空间分段放。以后多一个 namespace 就多一段。
[lf]
ADMIN_TOKEN=在这里填 wdl 部署 token
